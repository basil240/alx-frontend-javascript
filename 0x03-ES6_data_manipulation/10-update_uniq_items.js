const map = groceriesList();
console.log(map);

updated_map = {}
for item, quantity in gracery_map.items()
    if quantity == 1:
        updated_map[item] =100
    else:
        updated_map[item] = quantity

        return updated_map

